import assert from "assert";
import { makePuzzle, Puzzle } from "./Puzzle";

describe('Puzzle', async function () {

    // Testing strategy
    // 
    // 1) fetch /echo to check if the server is running
    // 
    // 2) fetch /getpuzzle and pass the string to makePuzzle to see if 
    //    it returns a valid string representation

    // Manual testing:
    //  1) No manual testing needed since the test suite already covers every case
    //     However, if the automatic ts fails, manually checking the results for 
    //     /getpuzzle might be helpful for debugging
});
